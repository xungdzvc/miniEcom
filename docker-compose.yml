version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    container_name: ecommerce_backend

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    container_name: ecommerce_frontend
